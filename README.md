# 间谍危机 SPYFALL

纯前端单手机传阅发牌 MVP，支持 3–8 人、20 个地点、秘密查看、防重复查看与 8 分钟倒计时。

## 运行

直接打开 `index.html`，或在项目目录运行 `python -m http.server 8080` 后访问 `http://localhost:8080`。

## 约束

无后端、账号、联网同步或数据库；本局数据仅保留在当前页面内。
